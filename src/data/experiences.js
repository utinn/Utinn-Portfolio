/**
 * Experience entries shared by the Home timeline (ANIMATION_SPEC.md 19) and
 * the dedicated Experiences Page groups (ANIMATION_SPEC.md 20).
 *
 * Order must match Figma's left-to-right spatial order — do not reorder by
 * assumed chronology (ANIMATION_SPEC.md 19.4).
 *
 * `images` supports the dedicated page's supporting-image rotation
 * (ANIMATION_SPEC.md 20.12 / INTERACTION_SPEC.md 19).
 *
 * Shape: { id, title, organization, date, image, images, tags, description }
 */
export const experiences = []
