import PageContainer from '../components/layout/PageContainer'

/**
 * Projects landing page shell. Hosts the curtain navigation system
 * (ANIMATION_SPEC.md Section 18, INTERACTION_SPEC.md Section 13) once its
 * Figma frame and curtain assets are available.
 */
export default function Projects() {
  return (
    <PageContainer>
      <h1 className="text-display font-sans">Projects</h1>
    </PageContainer>
  )
}
