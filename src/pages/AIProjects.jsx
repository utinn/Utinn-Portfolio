import PageContainer from '../components/layout/PageContainer'

/**
 * AI Projects page shell (child view of Projects — INTERACTION_SPEC.md
 * Section 15). Uses the return-curtain on the left (ANIMATION_SPEC.md
 * 18.12) once implemented.
 */
export default function AIProjects() {
  return (
    <PageContainer>
      <h1 className="text-display font-sans">AI Projects</h1>
    </PageContainer>
  )
}
