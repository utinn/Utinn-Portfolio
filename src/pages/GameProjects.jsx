import PageContainer from '../components/layout/PageContainer'

/**
 * Game Projects page shell (child view of Projects — INTERACTION_SPEC.md
 * Section 14). Uses the return-curtain on the right (ANIMATION_SPEC.md
 * 18.12) once implemented.
 */
export default function GameProjects() {
  return (
    <PageContainer>
      <h1 className="text-display font-sans">Game Projects</h1>
    </PageContainer>
  )
}
